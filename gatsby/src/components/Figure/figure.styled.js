import styled from 'styled-components';

const StyledFigure = styled.figure`
  text-align: center;

  figcaption {
    text-align: center;
    padding-top: 0.5rem;
    font-size: smaller;
  }
`

export {
  StyledFigure,
}